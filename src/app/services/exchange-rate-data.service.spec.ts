import { ExchangeRateDataService } from './exchange-rate-data.service';

describe('ExchangeRateDataService', () => {
  // These additional tests could be added:
  // describe('setSelectedCurrency()', () => {
  //   it(
  //     'should call http client concatenating of the endpoint constant and the passed currency'
  //   );
  //
  //   it('should, on http response, emit from data$ with the response data');
  // });
});

import { AppComponent } from './app.component';

describe('AppComponent', () => {
  // These additional tests could be added
  // it(
  //   'should call service.setSelectedCurrency on form currencySelected event, passing the value'
  // );
  // it('should bind service.data$ to table component data input');
});
